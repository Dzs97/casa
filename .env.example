# Vercel KV (Redis serverless) - create from Vercel Storage tab
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
KV_URL=

# Si no defines las anteriores, la app usa un store en memoria (datos se pierden al reiniciar el server)
